<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/newsList">

    <fragment
        android:id="@+id/newsList"
        android:name="com.saber.sabernews.presentation.features.news.NewsFragment"
        android:label="HomeFragment">
        <action
            android:id="@+id/viewNewsDetails"
            app:destination="@id/newsDetails" />
    </fragment>

    <fragment
        android:id="@+id/newsDetails"
        android:name="com.saber.sabernews.presentation.features.news.NewsDetailsFragment"
        android:label="NewsDetailsFragment"/>

</navigation>